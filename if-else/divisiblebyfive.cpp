#include<iostream>
using namespace std;
int main(){
  
// Q. Take Number as input from user and check if number is divisible by 5 or not print it on the screen.
    
    int n;
    cout<<"Enter a Number :";
    cin>>n;

    if(n%5==0) cout<<"Number is divisible by 5";
    else cout<<"Number is not divisible by 5";     
   
}